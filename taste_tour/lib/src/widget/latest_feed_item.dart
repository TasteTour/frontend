import 'package:flutter/material.dart';

class LatestFeedItem extends StatelessWidget {
  const LatestFeedItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
